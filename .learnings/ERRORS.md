## [ERR-20260712-001] git_branch_creation

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: infra

### Summary
Creating a local branch is blocked by the workspace sandbox's read-only `.git` metadata policy.

### Error
```
fatal: cannot lock ref 'refs/heads/m5stack-core-esp32-16mb': Unable to create '/Users/lgl/Code/esp8266-ai/.git/refs/heads/m5stack-core-esp32-16mb.lock': Operation not permitted
```

### Context
- Command: `git switch -c m5stack-core-esp32-16mb`
- The source worktree is writable; only `.git/refs` requires an escalated Git command.

### Suggested Fix
Run the scoped `git switch -c` command with sandbox escalation.

### Metadata
- Reproducible: yes
- Related Files: .git/refs/heads

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Use the environment's scoped Git authorization for the branch operation.

---

## [ERR-20260712-011] display_mode_post_503

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: medium
**Status**: resolved
**Area**: tests

### Summary
The display-mode API returned HTTP 503 once during persistence verification, while bridge and quota settings persisted correctly.

### Error
```
POST /api/display mode=codex -> HTTP 503
```

### Context
- The device was concurrently polling the bridge every second.
- `POST /api/bridge` and `POST /api/quota-display` returned `ok`.

### Suggested Fix
Bypass the local Surge proxy for direct requests to the device's private LAN address.

### Metadata
- Reproducible: unknown
- Related Files: firmware/src/main.cpp

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: The 503 page was generated by Surge Mac, not the ESP32 WebServer. The device handler now replies before deferred TFT repainting; direct POST and post-reset persistence both succeeded.

---

## [ERR-20260712-010] serial_boot_mode_diagnostic

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: medium
**Status**: resolved
**Area**: tests

### Summary
A manual serial-control sequence entered ESP32 download mode instead of performing a normal application reset.

### Error
```
rst:0x1 (POWERON_RESET),boot:0x7 (DOWNLOAD_BOOT...)
waiting for download
```

### Context
- The sequence was used only to capture startup LittleFS logs.
- No Flash write was performed.

### Suggested Fix
Use esptool's standard hard reset to return to normal boot, and avoid hand-crafted DTR/RTS reset sequencing for diagnostics.

### Metadata
- Reproducible: yes
- Related Files: firmware/src/main.cpp

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Run a read-only esptool command with its default hard reset.

---

## [ERR-20260712-009] littlefs_persistence_verification

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: high
**Status**: resolved
**Area**: config

### Summary
LittleFS-backed settings still reset after reboot even with first-boot formatting enabled.

### Error
```
Before reset: bridge=192.168.39.59:8765, mode=codex
After reset:  bridge="", mode=auto, last_update_s=-1
```

### Context
- The firmware was rebuilt and uploaded with `LittleFS.begin(true)`.
- A direct hardware reset was performed after writing settings through the device API.

### Suggested Fix
Verify the ESP32's actual flash capacity and the installed partition table; correct the LittleFS partition if it is outside physical flash or otherwise incompatible.

### Metadata
- Reproducible: yes
- Related Files: firmware/platformio.ini, firmware/partitions_m5stack_16mb.csv, firmware/src/main.cpp

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Mounted/formatted the explicitly labeled `littlefs` partition. Verified bridge, quota-display, and display-mode settings survive a hardware reset.

---

## [ERR-20260712-008] device_info_after_bridge_update

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: tests

### Summary
The device returned HTTP 503 once when queried immediately after its bridge host was updated.

### Error
```
curl: (22) The requested URL returned error: 503
```

### Context
- `POST /api/bridge` to the M5 returned `ok`.
- The next diagnostic read was issued three seconds later while the device began its first bridge poll.

### Suggested Fix
Retry the read after the first bridge poll and inspect the persisted bridge and update-age fields.

### Metadata
- Reproducible: unknown
- Related Files: firmware/src/main.cpp

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: A retry is required to distinguish temporary server unavailability from an ongoing polling issue.

---

## [ERR-20260712-007] serial_reenumeration

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: tests

### Summary
The USB serial device became unavailable while sampling logs immediately after the board reset.

### Error
```
serial.serialutil.SerialException: read failed: [Errno 6] Device not configured
```

### Context
- The uploader had already completed all writes and hash verification, then reset the ESP32 through RTS.

### Suggested Fix
Rediscover the serial device after reset before starting another monitor session.

### Metadata
- Reproducible: unknown
- Related Files: firmware/.pio/build/m5stack-core-esp32/firmware.bin

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: The firmware upload itself completed successfully before this read-only verification step.

---

## [ERR-20260712-006] platformio_monitor_non_tty

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: tests

### Summary
PlatformIO's interactive serial monitor cannot initialize in the non-TTY command session.

### Error
```
termios.error: (19, 'Operation not supported by device')
```

### Context
- Firmware upload completed successfully; only interactive log inspection failed.

### Suggested Fix
Use a non-interactive pyserial reader for bounded post-upload log sampling.

### Metadata
- Reproducible: yes
- Related Files: firmware/.pio/build/m5stack-core-esp32/firmware.bin

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Read the serial device directly without terminal control sequences.

---

## [ERR-20260712-005] serial_monitor_timeout

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: tests

### Summary
The macOS environment does not provide the GNU `timeout` command used to bound serial-monitor output.

### Error
```
zsh:1: command not found: timeout
```

### Context
- Attempted to read post-upload serial logs for 15 seconds.

### Suggested Fix
Run PlatformIO's monitor in an interactive session and stop it after the desired observation window.

### Metadata
- Reproducible: yes
- Related Files: firmware/.pio/build/m5stack-core-esp32/firmware.bin

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Use an interactive monitor session rather than `timeout`.

---

## [ERR-20260712-004] image_header_helper

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: tests

### Summary
A one-line Python helper for image-header reporting had an f-string quoting syntax error.

### Error
```
SyntaxError: f-string expression part cannot include a backslash
```

### Context
- The preceding `xxd` output successfully reported the header as `e9 05 02 4f`.

### Suggested Fix
Avoid escaped quotes inside an f-string expression.

### Metadata
- Reproducible: yes
- Related Files: firmware/.pio/build/m5stack-core-esp32/firmware.bin

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Header byte `0x4f` encodes the 16MB flash-size field.

---

## [ERR-20260712-003] esptool_direct_execution

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: tests

### Summary
The packaged `esptool.py` is not executable directly in this environment.

### Error
```
zsh:1: permission denied: /Users/lgl/.platformio/packages/tool-esptoolpy/esptool.py
```

### Context
- Attempted binary-header inspection after a successful PlatformIO build.

### Suggested Fix
Invoke the script through PlatformIO's Python interpreter instead.

### Metadata
- Reproducible: yes
- Related Files: firmware/.pio/build/m5stack-core-esp32/firmware.bin

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Use `~/.platformio/penv/bin/python .../esptool.py`.

---

## [ERR-20260712-002] platformio_build_cache

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: tests

### Summary
PlatformIO cannot create its global package/cache lock files from the sandbox.

### Error
```
PermissionError: [Errno 1] Operation not permitted: '/Users/lgl/.platformio/platforms.lock'
```

### Context
- Command: `~/.platformio/penv/bin/pio run`
- PlatformIO stores dependency locks and telemetry state under `~/.platformio`.

### Suggested Fix
Run the scoped build command with sandbox escalation; source files remain in the workspace.

### Metadata
- Reproducible: yes
- Related Files: firmware/platformio.ini

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Use the environment's scoped PlatformIO build authorization.

---
## [ERR-20260712-003] macos_app_install

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: infra

### Summary
First attempt to replace the installed app used an unsupported `ditto --delete` option.

### Error
```
ditto: unrecognized option `--delete'
```

### Context
- Replacing `/Applications/AI Clock Bridge.app` with the freshly packaged bundle.

### Suggested Fix
Remove the destination bundle explicitly, then use `ditto --rsrc`.

### Metadata
- Reproducible: yes
- Related Files: mac-app/scripts/package-macos-app.sh

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Replaced the bundle with `rm -rf` followed by `ditto --rsrc`; codesign verification passed.

---
## [ERR-20260712-004] build_wrong_workdir

**Logged**: 2026-07-12T00:00:00+08:00
**Priority**: low
**Status**: resolved
**Area**: infra

### Summary
Combined firmware/client build was first run from the repository root instead of the PlatformIO project directory.

### Error
```
NotPlatformIOProjectError: Not a PlatformIO project. platformio.ini file has not been found in current working directory
```

### Context
- Firmware project is under `firmware/`; macOS Swift package is under `mac-app/`.

### Suggested Fix
Run PlatformIO and Swift builds in their respective project directories.

### Metadata
- Reproducible: yes
- Related Files: firmware/platformio.ini, mac-app/Package.swift

### Resolution
- **Resolved**: 2026-07-12T00:00:00+08:00
- **Notes**: Rebuilt successfully from both project directories.

---
